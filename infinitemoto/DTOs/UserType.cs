namespace infinitemoto.DTOs
{
    public enum UserType
    {
        Unknown = 0, // Default value for unknown or invalid user types
        SuperAdmin = 1,
        Admin = 2,
        User = 3

       // Add other user types as needed      
    }
}
